# 2026-07-26 工作紀錄 — 發布鏈路修復（四層已修，第五層未修）

給接手的 agent。**先讀「未完成」一節**，那是唯一還在壞的東西。

## 結果

| | 修前 | 修後 |
|---|---|---|
| `daily-data.yml` | failure | **success** |
| `market-universe.yml` | failure（每 ~2h） | **success** |
| `deploy.yml` | skipped | **failure**（見未完成） |
| market_universe 新鮮度 | 10.1h（上限 3h） | **0.0h** |
| snapshot 日期 | 卡在 07-25 | **07-26** |

資料層已恢復更新。**網站尚未恢復部署。**

## 已修四層（皆已 CI 實測）

### 1. `1322b26` DeFi/Meme 板塊擋住整條發布線

各聚合商對這兩個籃子的成分定義不同（CoinGecko 的 DeFi ≠ CoinPaprika 的），
跨源報酬差距結構性偏大；平時在 1pp 內，那兩個板塊波動大時就超標。
兩個邊緣板塊因此 hard fail 掉整條線。

改為登錄 degradation。**門檻值未動**，資料仍標記為未跨源驗證。
對照驗證：注入 `single_source` 到 DeFi（豁免）與 Layer 1（非豁免），
前者進 degradations、後者進 failures，嚴格路徑完好。

### 2. `ebef4c5` ETF failover 測試的時間炸彈

`etf_quorum_passes` 的閘門之一是 `0 <= market_age_days <= 5`，
而 `test_source_failover.py` 的 fixture 日期寫死 `2026-07-17/20/21`
（2026-07-22 commit `4ae6533` 寫入，當時還新鮮）。六天後必炸。

失敗時的實測輸入——只有一項不合格：

```
completeness 1.0 | official_gap 0.0 | coverage 1.0
backup_gap 0.0 | same_date True | reconciled True | sources 3
market_age_days 6          ← 唯一失敗項
```

它是 `market-universe.yml` 的**第一步**，所以整個 hourly 在收集任何資料前就中止。
已改為相對 `today` 生成，維持測試真正要驗的相對結構（T+1 官方持倉未發布 → 回退 T）。

**通則：這個 repo 任何 fixture 都不可寫死日期。** 產線程式有多處相對時間閘門。

### 3. `f03eb94` ETF 真實跨源分歧擋住整條線

實測三個候選日各倒在不同閘門，因此全部落選：

```
07-23 age3  official_gap 0.78% ✓  coverage 94.7% ✓  backup_gap 6.39% ✗
07-22 age4  official_gap 0.89% ✓  coverage 24.5% ✗  backup_gap 1.40% ✓
07-21 age5  official_gap 3.40% ✓  coverage 57.0% ✓  backup_gap 7.61% ✗
```

已另外驗證 `ishares_holding` 對 07-21/22/23 **都抓得到**（07-24 是真的 T+1 未發布），
所以不是抓取失敗，是 The Block 與 Bitbo 對具名基金歸屬的真實 6–7% 分歧。
落選後回退到 07-20（age 6 → 超過閘門）才是壓垮整條線的那一下。

改為 degradation。**門檻值一律未動**，ETF 不得作為 hard trigger 的限制
（`etf_status != sample_cross_source_verified` 分支）也未動。

### 4. `2b62205` degraded 拒收更嚴格的發布模式

**這一層是第 3 層引發的**，先前不可達。三個模式依揭露多寡排序：
`full` > `research_only` > `diagnostics_only`。
`daily-data.yml` 的 degraded 分支只接受 `research_only`，
於是選了**更嚴格**的 `diagnostics_only` 反而被判違規而中止。

兩個維度獨立：`generate_institutional_analytics.py:597` 只要
`not logic_consistent` 就升級成 `diagnostics_only`，與 verification 狀態無關。
實測即為此組合：`logic_audit status=review, failed_invariants=1`。

已改為接受「research_only 或更嚴格」。`degraded + full`、`degraded + normal`
仍 hard fail——比要求寬鬆依然違規。

## ⚠️ 未完成：前端 degraded 狀態渲染（第五層）

`deploy.yml` 仍失敗。36 項稽核中 6 項失敗：

```
desktop/mobile  market-monitor.html               市場雷達可讀資料被錯誤封鎖
desktop/mobile  market-monitor.html:freshness-window  把批次內合格來源誤判為瀏覽時逾時
desktop         index.html:degraded               degraded fixture 未呈現預期狀態 degraded
desktop         dashboard.html:degraded           degraded fixture 未呈現預期狀態 degraded
```

**成因推測（未證實）**：`degraded` 這條路先前實際上從沒被走過——
系統只會落在 `pass` 或 `fail`。第 3 層把 ETF 改成降級後，才第一次真的產生
`verification=degraded`，於是前端沒實作／實作錯的 degraded 分支第一次曝光。

**已排除的假設**（不要重走）：

- `index.html:109` 的 `effectiveDegraded` 邏輯**是對的**：
  `verify.status==="degraded"` → `renderStatus="degraded"`、`conclusionsVisible="true"`。
- `verificationReadable` **已接受 degraded**：
  `["pass","degraded"].includes(verify.status) && (verify.failures||[]).length===0 && verifyBound`。

所以問題在更早的守衛。`pageReadable` 由六個條件組成，任一為 false 就走 `failClosed()`
（`index.html:83` 設 `renderStatus="fail"`）：

```
logicSafe        logic.status==="consistent" && analytics.quality.logic_audit_status===logic.status
                 && analytics.logic_audit.status===logic.status
logicBound       logic.snapshot_generated_at === latest.generated_at
dateSafe         analytics.quality.verification_date_matches_snapshot === true
analyticsBound   analytics.snapshot_generated_at === latest.generated_at
                 && analytics.quality.verification_bound_to_snapshot === true
statusBound      analytics.quality.verification_status === verify.status
verificationReadable  （見上，已排除）
```

**最可疑的是 `logicSafe`**：它要求 `logic.status === "consistent"`，
而production 現在是 `review`（`failed_invariants=1`）。這會讓
`market-monitor.html` 那兩項（用真實資料）失敗。但 `index.html:degraded`
用的是 fixture，其 `logic` 未被覆寫，需另外確認 fixture 的 logic.status。

**驗收標準是現成的**（這是好消息）：smoke test 已定義好 degraded fixture 與期望值，
見 `scripts/smoke_product_surfaces.py:344-350`：

```python
"verification": {**verification, "status": "degraded", "failures": [], "degradations": [...]},
"analytics":    {**analytics, "quality": {**quality, "verification_status": "degraded"}},
"expected_status": "degraded",
"should_show": True,
```

**復現指令**：

```bash
python3.11 scripts/smoke_product_surfaces.py     # 需要 playwright + chrome
```

## 環境陷阱（今天在這裡浪費最多時間）

**必須用 Python 3.11+ 重現，CI 是 3.12。** 本機 Python 3.9 無法解析 Coinbase 的
奈秒時間戳（`2026-07-25T17:43:51.221430410Z`，9 位小數），會憑空多出約 27 個
不存在的失敗。我一度回報「42 個失敗」，其中 27 個純粹是我自己的 Python 造成的。

**跑順序必須與 CI 一致**：`daily_data_pipeline.py` → `collect_market_universe.py`
→ `verify_daily_data.py`。順序反了會得到 4 個批次綁定失敗的假象。

**判斷 repo 是否停更前先 `git fetch`。** 本機 clone 曾落後 79 個 commit，
我因此一度誤判整個 repo 已停擺 11 天。

## 其他今日變更

- `frameworks/`（`7cfe130`）：曼報 KB 分析框架的唯讀副本 + 使用入口。
  自動化層完全沒有 LLM 呼叫，所以框架的消費端是判斷層文件
  （monitor-log、thesis-card、analysis-*），不是程式。
  **不要在 `frameworks/manny_skills/` 內編輯**，改上游
  `HsinTiger/manny-li-pro-kb` 的 `skills/` 後重跑 `sync-skills.sh`。
- 診斷期間動到的 `data/` 檔案一律 `git checkout` 還原，未進 commit。
